// examples/demo.ltl — runnable demo for ltl_gcd_ext

import std::io
use ltl_gcd_ext as lib

fn main() {
    io::println("ltl_gcd_ext demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}