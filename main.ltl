// ltl-gcd-ext/main.ltl
//
// GCD, extended GCD, and modular inverse over Z.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-gcd-ext v0.1.0")
    io::println("GCD, extended GCD, and modular inverse over Z.")
    0
}
